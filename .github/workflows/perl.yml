name: Perl CI

on:
  push:
    branches: [ "develop", "f/202405-pipeline-github" ]
  pull_request:
    branches: [ "develop" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: chdir
      run: cd libtest-module-runnable-perl/
    - name: ExtUtils::MakeMaker
      run: perl Makefile.PL
    - name: make
      run: make
    - name: make test
      run: make test
