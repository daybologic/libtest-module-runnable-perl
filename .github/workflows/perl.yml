name: Perl CI

on:
  push:
    branches: [ "develop", "f/202405-pipeline-github" ]
  pull_request:
    branches: [ "develop" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: make
      run: make
    - name: make test
      run: make test
